debugpy==1.8.8
bpy==4.2
pytest==8.3.3
pytest-dotenv==0.5.2
pytest-cov==6.0.0
pytest-html==4.1.1
pytest-xdist==3.6.1 # we need this so we can run tests in parallel without the multiple registration error with bindings pytest -n 1
poly-hammer-utils
numpy==2.2.0 #
-r ./src/addons/meta_human_dna/requirements.txt